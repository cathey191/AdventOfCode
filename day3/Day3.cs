using System;

class Day3
{
    static void Main()
    {
        var logFile = File.ReadAllLines(@"input.txt");
        var logList = new List<string>(logFile);

        for (int i = 0; i < binaryInp[0].Length; i++)
        {
            for (int j = 0; j < binaryInp.Length; j++)
            {
                
            }
        }
        
        System.Console.WriteLine("Result = " + logList);
    }
}